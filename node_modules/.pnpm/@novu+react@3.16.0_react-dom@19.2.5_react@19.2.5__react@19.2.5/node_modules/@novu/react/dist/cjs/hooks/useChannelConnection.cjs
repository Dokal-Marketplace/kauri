"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/hooks/useChannelConnection.ts
var useChannelConnection_exports = {};
__export(useChannelConnection_exports, {
  useChannelConnection: () => useChannelConnection
});
module.exports = __toCommonJS(useChannelConnection_exports);
var import_react = require("react");
var import_NovuProvider = require("./NovuProvider.cjs");
var useChannelConnection = (props) => {
  const novu = (0, import_NovuProvider.useNovu)();
  const propsRef = (0, import_react.useRef)(props);
  propsRef.current = props;
  const [connection, setConnection] = (0, import_react.useState)();
  const [error, setError] = (0, import_react.useState)();
  const [isLoading, setIsLoading] = (0, import_react.useState)(true);
  const [isFetching, setIsFetching] = (0, import_react.useState)(false);
  const fetchConnection = (0, import_react.useCallback)(
    async (options) => {
      const { identifier, onSuccess, onError } = propsRef.current;
      if (options == null ? void 0 : options.refetch) {
        setError(void 0);
        setIsLoading(true);
      }
      setIsFetching(true);
      const response = await novu.channelConnections.get({ identifier });
      if (response.error) {
        setError(response.error);
        onError == null ? void 0 : onError(response.error);
      } else if (response.data !== void 0) {
        onSuccess == null ? void 0 : onSuccess(response.data);
        setConnection(response.data);
      }
      setIsLoading(false);
      setIsFetching(false);
    },
    [novu]
  );
  (0, import_react.useEffect)(() => {
    const cleanupGetPending = novu.on("channel-connection.get.pending", ({ args }) => {
      if (!args || args.identifier !== propsRef.current.identifier) {
        return;
      }
      setIsFetching(true);
    });
    const cleanupGetResolved = novu.on("channel-connection.get.resolved", ({ args, data, error: resolvedError }) => {
      if (!args || args.identifier !== propsRef.current.identifier) {
        return;
      }
      const { onSuccess, onError } = propsRef.current;
      if (resolvedError) {
        setError(resolvedError);
        onError == null ? void 0 : onError(resolvedError);
      } else {
        setConnection(data ?? null);
        onSuccess == null ? void 0 : onSuccess(data ?? null);
      }
      setIsFetching(false);
    });
    const cleanupDeleteResolved = novu.on("channel-connection.delete.resolved", ({ args }) => {
      var _a, _b;
      if (!args || args.identifier !== propsRef.current.identifier) {
        return;
      }
      setConnection(null);
      (_b = (_a = propsRef.current).onSuccess) == null ? void 0 : _b.call(_a, null);
      setIsFetching(false);
    });
    void fetchConnection({ refetch: true });
    return () => {
      cleanupGetPending();
      cleanupGetResolved();
      cleanupDeleteResolved();
    };
  }, [novu, fetchConnection, props.identifier]);
  const refetch = (0, import_react.useCallback)(() => fetchConnection({ refetch: true }), [fetchConnection]);
  return {
    connection,
    error,
    isLoading,
    isFetching,
    refetch
  };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  useChannelConnection
});
//# sourceMappingURL=useChannelConnection.cjs.map