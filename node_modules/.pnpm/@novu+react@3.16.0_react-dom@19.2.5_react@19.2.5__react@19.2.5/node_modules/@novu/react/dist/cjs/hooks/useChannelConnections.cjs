"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/hooks/useChannelConnections.ts
var useChannelConnections_exports = {};
__export(useChannelConnections_exports, {
  useChannelConnections: () => useChannelConnections
});
module.exports = __toCommonJS(useChannelConnections_exports);
var import_react = require("react");
var import_NovuProvider = require("./NovuProvider.cjs");
var useChannelConnections = (props = {}) => {
  const novu = (0, import_NovuProvider.useNovu)();
  const propsRef = (0, import_react.useRef)(props);
  propsRef.current = props;
  const [connections, setConnections] = (0, import_react.useState)([]);
  const [error, setError] = (0, import_react.useState)();
  const [isLoading, setIsLoading] = (0, import_react.useState)(true);
  const [isFetching, setIsFetching] = (0, import_react.useState)(false);
  const fetchConnections = (0, import_react.useCallback)(
    async (options) => {
      const { onSuccess, onError, ...listArgs } = propsRef.current;
      if (options == null ? void 0 : options.refetch) {
        setError(void 0);
        setIsLoading(true);
      }
      setIsFetching(true);
      try {
        const response = await novu.channelConnections.list(listArgs);
        if (response.error) {
          setError(response.error);
          onError == null ? void 0 : onError(response.error);
        } else if (response.data !== void 0) {
          setConnections(response.data);
          onSuccess == null ? void 0 : onSuccess(response.data);
        }
      } catch (err) {
        setError(err);
        onError == null ? void 0 : onError(err);
      } finally {
        setIsLoading(false);
        setIsFetching(false);
      }
    },
    [novu]
  );
  (0, import_react.useEffect)(() => {
    const cleanupDeleteResolved = novu.on("channel-connection.delete.resolved", () => {
      void fetchConnections({ refetch: true });
    });
    void fetchConnections({ refetch: true });
    return () => {
      cleanupDeleteResolved();
    };
  }, [novu, fetchConnections]);
  const refetch = (0, import_react.useCallback)(() => fetchConnections({ refetch: true }), [fetchConnections]);
  return {
    connections,
    error,
    isLoading,
    isFetching,
    refetch
  };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  useChannelConnections
});
//# sourceMappingURL=useChannelConnections.cjs.map