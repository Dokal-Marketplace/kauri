import { ListChannelConnectionsArgs, ChannelConnectionResponse, NovuError } from '@novu/js';

type UseChannelConnectionsProps = ListChannelConnectionsArgs & {
    onSuccess?: (data: ChannelConnectionResponse[]) => void;
    onError?: (error: NovuError) => void;
};
type UseChannelConnectionsResult = {
    connections: ChannelConnectionResponse[];
    error?: NovuError;
    isLoading: boolean;
    isFetching: boolean;
    refetch: () => Promise<void>;
};
/**
 * List channel connections for the current subscriber. Use this to discover an active
 * connection identifier after a chat OAuth flow so it can be passed to `LinkUser`.
 *
 * @example
 * const { connections } = useChannelConnections({ integrationIdentifier: 'my-chat-integration' });
 * const connectionIdentifier = connections[0]?.identifier;
 */
declare const useChannelConnections: (props?: UseChannelConnectionsProps) => UseChannelConnectionsResult;

export { type UseChannelConnectionsProps, type UseChannelConnectionsResult, useChannelConnections };
