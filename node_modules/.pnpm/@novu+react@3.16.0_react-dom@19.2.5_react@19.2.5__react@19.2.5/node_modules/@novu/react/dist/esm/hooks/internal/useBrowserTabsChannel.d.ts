declare const useBrowserTabsChannel: <T = unknown>({ channelName, onMessage, enabled, }: {
    channelName: string;
    onMessage: (args: T) => void;
    enabled?: boolean;
}) => {
    postMessage: (data: T) => void;
};

export { useBrowserTabsChannel };
